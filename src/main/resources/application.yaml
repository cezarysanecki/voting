spring:
  datasource:
    url: jdbc:mysql://localhost:3306/voting
    username: voting
    password: voting01
  jpa:
    hibernate:
      ddl-auto: create
  liquibase:
    change-log: db/changelog/db.changelog-master.xml
